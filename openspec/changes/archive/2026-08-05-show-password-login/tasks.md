## 1. UI Implementation

- [x] 1.1 Update `frontend/src/pages/LoginPage.tsx` importing `Eye` and `EyeOff` from `lucide-react`, adding `showPassword` state, and adding toggle button to password field

## 2. Verification & Build Checks

- [x] 2.1 Run TypeScript typecheck and build on `frontend` (`npm run build`)
