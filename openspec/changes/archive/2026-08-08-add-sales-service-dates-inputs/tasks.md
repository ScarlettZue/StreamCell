## 1. Quick Sale Modal Service Dates (Frontend)

- [x] 1.1 Update `startDate` and `endDate` state variables in `frontend/src/pages/SalesPage.tsx` with setter functions (`setStartDate`, `setEndDate`) and helper function to automatically calculate +30 days on start date change
- [x] 1.2 Render 2 date inputs (`Fecha de Inicio` and `Fecha de Vencimiento`) in the Quick Sale modal form in `SalesPage.tsx`
- [x] 1.3 Reset date state values to default (today & +30 days) when the sale form is submitted or modal is reset

## 2. Verification & Build Checks

- [x] 2.1 Run TypeScript typecheck and build on `frontend` (`npm run build`)
